{{#flash}}
<div class="container">
  <div class="alert alert-block{{#type}} alert-{{type}}{{/type}}">
    <button type="button" class="close" data-dismiss="alert">×</button>
    {{#title}}<h4>{{title}}</h4>{{/title}}
    {{#msg}}{{msg}}{{/msg}}
    {{^msg}}{{flash}}{{/msg}}
  </div>
</div>
{{/flash}}