{{#flash}}
<div class="container">
  <div class="alert alert-block{{#flash.type}} alert-{{flash.type}}{{/flash.type}}">
    <button type="button" class="close" data-dismiss="alert">×</button>
    {{#flash.title}}<h4>{{flash.title}}</h4>{{/flash.title}}
    {{#flash.msg}}{{flash.msg}}{{/flash.msg}}
    {{^flash.msg}}{{flash}}{{/flash.msg}}
  </div>
</div>
{{/flash}}
{{{flash.title}}}